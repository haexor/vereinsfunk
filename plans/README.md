# Vereinsfunk – ausführbarer Umsetzungsplan

Stand: 2026-08-02. Das Repository besitzt noch keinen ersten Git-Commit (`unborn HEAD`). Deshalb enthält jedes Arbeitspaket Baseline-Hashes für seine wichtigsten Ausgangsdateien. Vor der Umsetzung müssen diese geprüft werden; bei Abweichungen gilt die jeweilige STOP-Bedingung.

## Zielbild

Vereinsfunk wird eine Content-Werkstatt für das gesamte Vereinsleben: Abteilungen erfassen echte Beobachtungen, Fakten, Zitate, Bilder und Clips; das System erstellt daraus markenkonforme Varianten, schützt Personen auf Medien, führt eine konkrete Version durch die Freigabe und veröffentlicht sie geplant auf Instagram und Facebook.

Die Systemgrenzen sind verbindlich:

- Nuxt ist die Arbeitsoberfläche.
- Supabase Auth, Postgres, RLS, Storage und Realtime sind die fachliche Source of Truth.
- Fastify ist die vertrauenswürdige Servergrenze für OAuth, Webhooks, Service-Role-Zugriffe, kurzlebige Medien-URLs und Workflow-Trigger.
- Hatchet ist die einzige technische Workflow- und Zeitplan-Engine. Hatchet-Nachrichten enthalten IDs und kleine Routing-Metadaten, keine Medien, Tokens oder vollständigen Fachobjekte.
- TypeScript-Worker führen Texterstellung, Bildbearbeitung, Remotion-Rendering, Veröffentlichung und spätere Analytics aus.
- `SocialPublisher` bleibt die Provider-Grenze; zuerst wird Meta direkt angebunden. Postiz/Mixpost werden im MVP nicht betrieben. Hive ist kein Ersatz für Hatchet und bleibt außerhalb des Systems.

## Reihenfolge und Abhängigkeiten

| Nr. | Arbeitspaket | Abhängigkeiten | Status |
|---|---|---|---|
| 001 | [Inhaltsmodell und authentische Erfassung](001-content-domain-and-authentic-capture.md) | keine | in Arbeit |
| 002 | [Private Medien, Einwilligungen und Freigabegate](002-private-media-consent-and-approval-gate.md) | 001 | in Arbeit; `schedule_publication` erzwingt jetzt einen konservativen Medien-Gate-Kern (scan/Gesichtsentscheidung/Consent/Derivat), `POST /v1/publications/:id/execute` prüft ihn vor jedem externen Aufruf erneut. Upload-Pfad, Worker-Normalisierung, Review-UI und die feingranulare Consent-Scope-/Textscan-Prüfung als Hard-Blocker bleiben offen, siehe Plan-Status-Nachtrag |
| 003 | [Kreative Gesichtsverdeckung für Bilder](003-creative-face-obscuring.md) | 002 | in Arbeit |
| 004 | [Hatchet produktionsreif integrieren](004-hatchet-production-orchestration.md) | keine; parallel zu 001–003 möglich | erledigt; lokaler ID-only Dispatcher/Worker-Nachweis, Outbox-/Run-CAS und Runbook vorhanden; **Plan 038** hat das strukturelle Operations-Gate seitdem geschlossen (Hatchet läuft jetzt in Produktion), der 30-Job-Fairnessnachweis selbst steht weiterhin aus |
| 005 | [Kreative Plattformvarianten und Rendering](005-creative-platform-variants-and-rendering.md) | 001, 002, 003, 004 | in Arbeit |
| 006 | [Direkte Meta-Veröffentlichung](006-direct-meta-publishing.md) | 002, 004, 005 | in Arbeit |
| 007 | [Pilotbetrieb, Messung und Go/No-Go](007-pilot-readiness-and-go-no-go.md) | 001–006 | blockiert: externe Pilot-/Provider-Gates |

Empfohlener Ablauf: zuerst 001; der technische Unterbau aus 004 ist abgeschlossen. Danach 002 und 003; anschließend 005 und 006; zum Schluss 007. Ein Paket wird erst als abgeschlossen markiert, wenn alle Done-Kriterien erfüllt sind. Statuswerte sind `bereit`, `in Arbeit`, `blockiert`, `erledigt`.

## Zweite Serie: Produktausbau und Rückbau der Prototyp-Daten

Stand: 2026-08-04, geplant auf `b5c2eda6`. Die Pakete 001–007 bauen die Inhalts- und Veröffentlichungskette. Die Serie 008–021 macht daraus ein Produkt, das ein Verein selbst einrichten und betreiben kann, und ersetzt dabei die Prototyp-Daten durch echte Workflows.

| Nr. | Arbeitspaket | Abhängigkeiten | Status |
|---|---|---|---|
| 008 | [Echte Authentifizierung und Autorisierungsgrenze](008-echte-authentifizierung-und-autorisierungsgrenze.md) | keine | erledigt |
| 009 | [Onboarding: Verein anlegen](009-onboarding-verein-anlegen.md) | 008 | erledigt |
| 010 | [Abteilungen, Teams, Mitglieder und Einladungen](010-abteilungen-teams-mitglieder-einladungen.md) | 008, 009 | erledigt |
| 023 | [Sichtbarkeit, Mitgliederverwaltung und Richtliniengrundlage](023-sichtbarkeit-mitgliederverwaltung-und-richtliniengrundlage.md) | 010 | erledigt |
| 011 | [Regelwerk: Freigaberouten, Vertrauen je Mitglied und Kontingente](011-regelwerk-richtlinien-und-kontingente.md) | 010, 023 | erledigt; Freigabe-/Einplanungsendpunkte sind echt, aber ohne die Inhalts-Pipeline (001–007) fehlt weiterhin der Weg, wie ein `post_version`-Datensatz entsteht — end-to-end erst nach 005/006 vorführbar |
| 012 | [Kanäle und Social-Accounts](012-kanaele-und-social-accounts.md) | 011 | erledigt; gegen Meta-Testkonten ungetestet (externes Gate, App Review vor Pilotbetrieb nötig) |
| 013 | [Marke, Branding-Assets und Schriften](013-marke-branding-assets-und-schriften.md) | 009 | erledigt; kuratierte Font-Registry bewusst kleiner als empfohlen (zwei statt sechs bis acht Familien, siehe Plan); Remotion-Pixelvergleich und Erfolgspfad-Storage-Test bleiben manuell statt automatisiert |
| 014 | [Integrationsrahmen und Mitgliederverzeichnis](014-integrationsrahmen-und-mitgliederverzeichnis.md) | 010 | erledigt; Produktivbetrieb ab 020 möglich; HTTP-Adapter und Foto-Upload fürs eigene Profil bewusst nicht enthalten; die globale Transaktion über externe Systeme fehlt weiterhin (siehe ADR-009 „Bekannte Grenzen“) |
| 015 | [Einwilligungsverwaltung](015-einwilligungsverwaltung.md) | 002, 014 | erledigt; Produktivbetrieb ab 020 möglich; „Übernahme aus Quellsystem“ (`origin='imported'`) bewusst nicht gebaut (kein Zielsystem mit Testzugang, wie beim HTTP-Adapter aus 014); Textprüfung nur regelbasiert, kein Sprachmodell (Content-Pipeline 001–007 fehlt weiterhin); siebter Befund (2026-08-11) behoben: `organization_consent_texts_immutable` ist auf `BEFORE UPDATE` beschränkt, damit die `ON DELETE CASCADE`-Löschung einer Organisation mit Einwilligungstext durchläuft |
| 016 | [Auswertung: interne Kennzahlen](016-auswertung-interne-kennzahlen.md) | 011 (technisch); 025 operativ | erledigt; alle vier Endpunkte (`summary`/`timeseries`/`breakdown`/`funnel`) berechnen live aus den Rohtabellen statt aus einem Cache (Plan vor dem Bauen entsprechend angepasst, siehe Plan „Abweichungen“); neue Tabelle `post_status_events` samt Aufbewahrungsregel; kritischer Fund der adversarialen Prüfung behoben (siehe unten). Abhängigkeit 011 ist die technische (Rechte-/Scope-Grundlage) — 025 ist keine Code-Abhängigkeit, liefert aber erst die echten `post`/`post_version`/`publication`-Zeilen, ohne die diese Auswertung nur einen leeren Funnel zeigt (CodeRabbit-Fund zu PR #28, klärt das Verhältnis zur Empfehlung in Zeile 56 unten). **PR #28 gemergt** (Commit `1883758f`) — Review-Fix zu 15 CodeRabbit-Funden vorher gepusht (Commit `c300cbcd`: quadratische Bucket-Schleife durch `computeCountMetricsSeries` ersetzt, `.in()`-Batching, sechs Plan-Dokument-Inkonsistenzen bereinigt) |
| 017 | [Plattform-Insights](017-plattform-insights.md) | 006, 016 | blockiert: Meta App Review |
| 018 | [Resonanz- und Sentimentanalyse](018-resonanz-und-sentimentanalyse.md) | 017 | blockiert: Rechtsgrundlage und AVV mit LLM-Anbieter |
| 019 | [Mannschaften, Spielpläne und Veranstaltungen](019-mannschaften-spielplaene-und-veranstaltungen.md) | 014 | erledigt; ursprünglich von der noch offenen PR #21 zu 014 abgezweigt, vor der PR-Erstellung auf den inzwischen gemergten `main` rebast (inkl. PR #21 und deren Review-Fix-Nachfolge-PR #22); vierter Anlassvorschlag („Kontingent unausgeschöpft“) bewusst nicht gebaut, siehe Plan |
| 020 | [Rechtliche Pflichten und Datenschutzbetrieb](020-rechtliche-pflichten-und-datenschutzbetrieb.md) | 009, 012, 015 | erledigt; sechs Aufbewahrungsregeln (inkl. Einwilligungsnachweise), Betroffenenrechte, Verarbeitungsdokumentation, manipulationssicherer Audit-Trail mit echter Signaturprüfung, öffentliches Impressum je Verein; Retention- und Signaturläufe bleiben bis zu einer separaten fachlichen Scheduler-Anbindung manuell auslösbar; vollständige Vereinskonto-Löschung bewusst nicht gebaut |
| 021 | [Abomodelle, Speicherkontingent und Nutzungsgrenzen](021-abomodelle-und-speicherkontingent.md) | 009, 010, 011 | erledigt: PR #61 (Migration), #62 (API), #63 (Oberfläche) alle gemergt (2026-08-13). Durchsetzung von `max_teams`/`max_departments`, ein Plattform-Admin-Editor für Tarife, und drei Beitragskontingente je Kalendermonat nach Medienherkunft statt Kosten/Format: eigene Beiträge, KI-Bilder, KI-Videos mit Längenobergrenze (Kosten-in-Cent- und Instagram-Format-Zwischenstände verworfen — für einen Verein verständlicher, macht den Recovery-Sonderfall aus Paket 035 überflüssig); KI-Bild-/Videokontingente bewusst wirkungslos, bis ein eigenes Paket die Erzeugung baut; Zahlungsabwicklung als eigenes Vorhaben |
| 024 | [Freigaberoute bewusst neu auflösen](024-freigaberoute-neu-aufloesen.md) | 011 | erledigt; schließt zugleich die seit Paket 011 offene Lücke, dass `request_approval` einen selbst gewählten Prüferkreis akzeptierte, statt der konfigurierten Richtlinie zu folgen |
| 025 | [Inhalts-Pipeline schließen: Entwurfserzeugung und Veröffentlichung ausführen](025-inhalts-pipeline-entwurf-und-veroeffentlichung.md) | 011, 012 | erledigt; schließt die seit 001/011/012/014/015/019 dokumentierte Lücke, dass kein Code eine `post`/`post_version` aus einer `submission` erzeugt; `POST /v1/publications/:id/execute` ruft `SocialPublisher.publish()` erstmals auf, aber ohne die Upload-Pipeline (002/003) hat jede so entstehende `post_version` keine Medien — ein echter Veröffentlichungsversuch schlägt deshalb korrekt mit „mindestens ein Medium erforderlich“ fehl, siehe Plan |
| 039 | [Vereinseigene Website/Blog als Kanal](039-vereins-website-als-kanal.md) | 012, 025, 042 | erledigt: PR 1 (**#78**) und PR 2 (**#79**) beide gemergt. Datenmodell, `POST /v1/channels` für Website-Kanäle, Zeichengrenze je Kanal (min() über verfügbare Kanäle je Plattform, plus Deckel durch die globale Plattform-Vorgabe), Token-Leine im Worker aus `max_characters` und Belegzahl abgeleitet; Formular „Eigene Website/Blog hinzufügen“ in `/kanaele`, Anzeige/Nachjustieren der Zeichengrenze in `ChannelCard.vue`, optionale Mitglieds-Obergrenze in `/erstellen`. Betreiberentscheidung: der eigene Blog ist **ein Kanal wie jeder andere** — vom Vereins-Admin angelegt, über `channel_scopes` den Abteilungen zugeteilt, mit eigener Längengrenze unterhalb der harten Obergrenze. Damit braucht die Textwerkstatt keinen Sonderfall, und ein Verein ohne Social-Kanal kann sie überhaupt erst benutzen (seit Paket 042 PR 3 war das eine Sackgasse). Der interaktive Browser-Durchlauf aus dem Plan (Kanal anlegen, Länge setzen, neu laden) ist nicht automatisiert verifiziert. Der **Auslieferungsmechanismus** (Feed, Webhook, CMS-Plugin, Einbettcode) bleibt bewusst ausgeklammert und braucht weiterhin die Ausgangslage-Recherche plus eine Betreiberentscheidung — danach eigenes Folgepaket. Ursprünglich als 037 nummeriert; diese Nummer war bereits durch Plattform-Personas belegt (PR #56/#58) — auf 039 korrigiert |

Empfohlener Ablauf: **008 und 009 unmittelbar hintereinander** — ohne Authentifizierung lässt sich kein Prototyp-Datensatz ehrlich ersetzen, und ohne Onboarding zeigt die Oberfläche nach der Anmeldung nichts. Danach 010, dann **023** und darauf 011 als Verwaltungsgrundlage, 013 parallel dazu. Anschließend 012, dann 014 mit 019 als erstem Nutzen des Integrationsrahmens, dann 015 — alle erledigt. **025** schließt danach die seit 001–007 offene Inhalts-Pipeline-Lücke (`submission` → `post`/`post_version`), womit 016 erstmals auf echten Beitragsdaten statt nur auf einer Pipeline-Lücke aufbauen konnte — **016 ist erledigt**, live berechnet statt vorberechnet (siehe Plan „Abweichungen“), da die Datenlage weiterhin dünn ist und ein Vorberechnungs-Cache ohne Hatchet-Cron nur tote Infrastruktur gewesen wäre. 017 und 018 hängen an externen Gates. **020 vor dem Produktivbetrieb mit echten Personendaten.** **021 ist erledigt** — Kontingent und Speichergrenze waren vor dem ersten zahlenden Verein nötig, sonst ist der Speicher eines geteilten Systems unbegrenzt beschreibbar. **024 ist erledigt**: der Ausweg aus einer festhängenden Freigabe (`reresolve_approval_route`) ist gebaut, `request_approval` leitet seine Route seitdem ausschließlich selbst ab.

## Dritte Ebene: Plattform-Administration (SaaS-Betreiber)

Orthogonal zur Serie 008–020: Diese Pakete betreffen den SaaS-Betreiber selbst, nicht einen einzelnen Verein. Sie sind nicht Teil der Rückbau-Kette (keine Prototyp-Daten werden ersetzt) und können unabhängig von der Reihenfolge oben eingeschoben werden.

| Nr. | Arbeitspaket | Abhängigkeiten | Status |
|---|---|---|---|
| 022 | [Plattform-Administration](022-plattform-administration.md) | 008, 009 | erledigt |
| 033 | [Echter Textgenerator und Provider-Routing](033-echter-textgenerator-und-provider-routing.md) | 032, 004 | erledigt; Text-only Worker-Pilot mit strukturierten Kandidaten und atomarer Provenienzübernahme. Bild-/Video-KI bleibt ausdrücklich deaktiviert; KI-Text-Kennzeichnung blockiert Publishing |

Hinweis zu 023: das Paket ist beim Review von 010 aus 011 herausgelöst worden (Sichtbarkeit, Mitglieder-Detailebene, Einladungsrecht als Richtlinie) und gehört **direkt nach 010**, vor 011 — es legt `policy_settings` und die Vererbungsauflösung an, die 011 dann erweitert. Die Nummer folgt nur dem nächsten freien Platz; eine Umnummerierung von 011–022 wäre teurer als diese Notiz. 012 nutzt `policy_scope` aus 023 mit.

Hinweis zu 011: das Paket baut die vollständige Regel-Engine (Migration, `authz`-Funktionen, Domain-Funktionen, API-Endpunkte, Oberfläche) für Freigaberouten, Vertrauen je Mitglied und Kontingente. Die Inhalts-Pipeline (Submission → Post → Post-Version), Teil der ersten Planserie (001–007), fehlte danach weiterhin — **✓ geschlossen in Paket 025**: `POST /v1/submissions` erzeugt bei vollständigem Quellmaterial jetzt echt einen `post`/eine `post_version`. Details und ein in der adversarialen Prüfung gefundener, in 011 selbst behobener kritischer Fund (`request_approval` vertraute Client-Eingaben blind) stehen in `plans/011-regelwerk-richtlinien-und-kontingente.md`, Abschnitt „Umsetzung: Ergebnis und Abweichungen vom Plan“.

Hinweis zu 021: die operative Speicher-/Kontingent-Übersteuerung und der neue Tarif-Editor laufen über `requirePlatformAdmin` (kein eigenes Recht `platform.manage`, siehe Plan-Update 2026-08-13) und setzen damit die in 022 gebaute Plattform-Admin-Identität voraus — die Package-Nummern zeigen hier in die falsche Reihenfolge.

## Vierte Serie: Review und nachhaltiges Refactoring

Stand: 2026-08-09, geplant auf `1883758f`. Diese Arbeitspakete folgen aus dem Code-Review und verändern keine Produktverträge ohne separate Entscheidung.

| Nr. | Arbeitspaket | Abhängigkeiten | Status |
|---|---|---|---|
| 026 | [Synchronisationsläufe serialisieren und idempotent machen](026-sync-runs-serialisieren-und-idempotent-machen.md) | keine | erledigt; atomarer Service-Role-Guard vor jedem Quellenabruf/Fachdaten-Lookup, `Idempotency-Key` mit Replay-Antwort und ein aktiver Apply-Slot je Quelle/Bereich; parallele Dry-Runs bleiben bewusst möglich; manuell auditiertes Abbrechen bestätigter Prozessabbrüche über `POST /v1/integration-sources/:id/sync-runs/:runId/cancel` |
| 027 | [Fastify-API in fachliche Route-Module zerlegen](027-api-route-module-boundaries.md) | 026 | erledigt; PR #35, #36, #38 alle gemergt. `app.ts` von 8.301 auf 222 LoC reduziert (nur noch Bootstrap/Registrierung), alle Domänen unter `apps/api/src/routes/` extrahiert. `routes/shared.ts` trägt die komplette, domänenübergreifend gebrauchte Regelauflösung (`fetchPolicyRuleRows`/`computeRuleEntry`/`fetchMemberTrust` u. a. — auch von `POST /v1/submissions` benötigt) sowie `isAnyMemberOfOrganization`/`checkRateLimit`/`createAuditRecorder` statt je Modul dupliziert zu werden. `app.test.ts` (6.561 LoC) ist in fünfzehn `*.routes.test.ts`-Dateien plus `testSupport.ts` aufgeteilt. Beim Abschluss zusätzlich entstanden: Service-Module (`services/integrationSync.ts`, `services/consent.ts`, `services/approvalRouting.ts`, `services/mediaGate.ts`), eine im Personen-Sync gefundene Duplikat-Fassung von `loadIgnoredFingerprints`/`buildPendingConflicts` gegenüber Paket 019 vereinheitlicht, und `setErrorHandler` korrigiert (nur 4xx aus Fastify-eigenen Fehlern wird übernommen, 5xx bleibt generisch statt Interna zu leaken) |
| 028 | [Web-API-Zugriffe vereinheitlichen und große Seiten zerlegen](028-web-api-client-und-grosse-seiten-zerlegen.md) | 027 | erledigt; testbarer API-Client mit verbindlichen Web-Tests, API-Mutationen von Marke/Mitgliedern/Integrationen vereinheitlicht; Rechts-/Kanal-/Mitglieder-Ladepfade sowie Audit-, Auftragsverarbeiter-, Vorschau- und fünf Integrations-Komponenten extrahiert. Alle fünf großen Seiten liegen unter 500 LoC, darunter `kanaele.vue` (55 LoC) als Composition Root über `useChannels` und `einstellungen/recht.vue` (492 LoC) mit ausgelagerten Formularen; sechs Web-Testdateien mit sechzehn Tests laufen, einschließlich erfolgreichen, schema-validierten Loads und erhaltenen Berechtigungsfehlern. Der authentifizierte Browser-Smoke-Test gegen einen frischen lokalen Stack ist erledigt; `useIntegrationSources` bleibt bewusst aus, weil die Seite sein einziger Aufrufer wäre. |
| 029 | [Contracts und Domain-Exports fachlich aufteilen](029-contracts-und-domain-exports-aufteilen.md) | 027 | erledigt (2026-08-12); `contracts/src/index.ts` (2.016 LoC) in dreizehn Fachmodule zerlegt (`content.ts`, `organization.ts`, `policy.ts`, `channels.ts` u. a.), `domain/src/index.ts` (560 LoC) in `postStatus`, `effectiveConfig`, `reviewRoute`, `channels`, `mediaGate`, `llmProviders`; Root-Barrel-Exports beider Pakete unverändert kompatibel, kein Importzyklus |
| 030 | [Verbotene Selbstfreigabe auch im Reviewer-Snapshot entfernen](030-reviewer-snapshot-ohne-autor.md) | keine | erledigt; effektive Reviewer ohne Autor werden im Snapshot zurückgegeben, einschließlich Minderjährigenstufe getestet |
| 031 | [Rollenauflösung batchen und die Stalled-Abfrage serverseitig eingrenzen](031-rollenaufloesung-batchen-und-stalled-abfrage-eingrenzen.md) | 027 | erledigt; `RoleProvider.rolesForScopes` (optional, `SupabaseRoleProvider` mit konstant bis zu 3 Abfragen statt einer je Abteilung/Team) plus Fallback `resolveRolesForScopes`, `routes/members.ts` löst die Capability-Felder jetzt in einem Aufruf auf. Neue `security_invoker`-View `stalled_approval_requests` (Migration `2026081501`) wertet „mindestens eine offene/festhängende Stufe ist überfällig oder die Anfrage ist invalidiert“ serverseitig aus statt alle `approval_requests` einer Organisation zu laden und in TypeScript zu filtern; pgTAP-Test bestätigt Filterung und RLS |
| 034 | [Ausgehende LLM-Provider-Aufrufe absichern und Kandidaten-Wiederherstellung nach Absturz](034-provider-aufrufe-absichern-und-generation-recovery.md) | 033, PR #50 | erledigt, Restlücke durch 035 geschlossen; neues Paket `@vereinsfunk/outbound-fetch` (verschobene Zieladressenprüfung plus `createGuardedFetch` als neuer, größenbegrenzter Default-Fetcher beider `StructuredContentGenerator`-Adapter, `apps/worker` unverändert), eine blockierte/umgeleitete Adresse klassifiziert jetzt nicht-wiederholbar als `provider_configuration`; `acquire_generation_candidate` erobert einen seit über 15 Minuten auf `generating` hängenden Kandidaten zurück und meldet für einen noch nicht reifen Kandidaten ehrlich `failed` statt fälschlich `succeeded`. Provider-seitige Idempotenz gegen einen theoretisch doppelt abgerechneten LLM-Aufruf bewusst zurückgestellt |
| 035 | [Von Hatchets Wiederholungsbudget unabhängiger Recovery-Trigger für hängende Textgenerierungs-Kandidaten](035-generation-recovery-trigger.md) | 034 | erledigt; Fencing-Token `generation_candidates.generation_lease_token` (analog `workflow_runs.worker_lease_token`), sichtbares Ausloeser-Feld `triggered_by` (`member`/`automatic_recovery`, in der Textwerkstatt-UI angezeigt), Obergrenze `composition_sessions.candidate_count` (Platzhalter 8, unkalibriert), neue Funktion `claim_stalled_generation_candidates` sowie ein eigener, bei Hatchet deklarativ per `onCrons` (`*/5 * * * *`) geplanter Workflow `generation-recovery-scan` (`apps/worker/src/generationRecovery.ts`), außerhalb von `WorkflowNameSchema`s Pro-Entity-Schleife registriert. Abweichung von der Ausplanung: `client.task(...)` akzeptiert `onCrons` direkt (Teil von `CreateBaseWorkflowOpts`, das `CreateTaskWorkflowOpts` mit einschließt) — die geplante `client.workflow(...)`-Variante war nicht nötig, per `tsc` verifiziert. Kontingent-Interaktion weiterhin nicht Teil dieses Plans — verweist auf eine künftige Erweiterung von Paket 021 |
| 036 | [Datenbank-Migrationen automatisch beim Container-Start anwenden](036-migrationen-beim-container-start-automatisieren.md) | keine (App-Repo); blockiert für den vollen Rollout auf eine separate PR im `ansible`-Repo (Step 4) | App-Repo-Teil erledigt; ausgelöst durch den produktiven Ausfall von `/plattform-admin` am 2026-08-12 (Migration `2026081208` war gemergt, aber nie gegen die Produktionsdatenbank angewendet — Watchtower aktualisiert Container unabhängig von Ansible, das Supabase bewusst nicht anfasst; tatsächlich fehlten 19 Migrationen seit einer Woche, alle nachgezogen). Boot-Hook (`packages/db-migrate`) in `apps/api`/`apps/worker` per `supabase db push`, volle `postgres`-Verbindung (mit dem Nutzer abgestimmt), hartes Fehlschlagen statt stillem Weiterlaufen bei kaputter Migration; beide Docker-Images lokal gebaut und der Boot-Hook end-to-end verifiziert. Verlangt noch eine begleitende PR im separaten `ansible`-Repository (Step 4, `DATABASE_URL` in `env.j2`/`docker-compose.yml.j2`/Quadlet-Templates) |
| 038 | [Hatchet produktiv betreiben (Operations-Gate aus Plan 004 schließen)](038-hatchet-produktionsbetrieb.md) | 004 | erledigt im `ansible`-Repo (Commits `ed096a2`/`f08a6c1`, 2026-08-13), unabhängig von dieser Codebasis nachvollzogen: eigenständige Rolle `roles/hatchet` auf haex.space, `vereinsfunk-worker` erreicht sie über ein gemeinsames Docker-Netzwerk statt der ursprünglich geplanten `host.docker.internal`-Loopback-Route (funktioniert auf nativem Linux-Dockerd nicht, siehe `docs/operations/hatchet.md`); Namensdrift `HATCHET_SERVER_URL`/`HATCHET_API_URL` → `HATCHET_CLIENT_HOST_PORT`/`HATCHET_CLIENT_API_URL` für den Docker-Pfad behoben, Quadlet-Pfad (haex.cloud, nicht aktiv) bewusst unverändert. `vereinsfunk-worker` läuft auf haex.space bestätigt stabil (`RestartCount=0`, `generation-recovery-scan` erfolgreich im Fünf-Minuten-Takt). Der 30-Job-Fairnessnachweis aus Plan 004 war nie Teil dieses Plans und steht weiterhin aus |
| 041 | [Prop-Mutation in Formular-Komponenten durch `defineModel` ersetzen](041-formular-komponenten-definemodel.md) | keine | PR #69 offen; ausgelöst durch einen CodeRabbit-Fund zu PR #67 (`StyleProfileEditorForm.vue`); sechs der sieben gefundenen Komponenten umgestellt (fünf mechanischer `defineModel`-Tausch, `ChannelCard.vue`/`MemberList.vue` zusätzlich mit Umstellung von Record-Dictionaries auf skalare Felder — bei `MemberList.vue`/`pages/mitglieder.vue` eine als verhaltensäquivalent begründete Zustandsvereinfachung, da nur je eine Mitgliedschaft gleichzeitig im Bearbeitungsmodus ist); `vue/no-mutating-props` als Regressionsschutz aktiv. `StyleProfileEditorForm.vue` folgt erst nach dem Merge von PR #67, da die Datei nur dort existiert |
| 042 | [LLM-Laufzeitparameter vom Provider zur Sitzung verschieben](042-llm-laufzeitparameter-vom-provider-zur-sitzung.md) | 033, 040, 022 | erledigt: PR 1 (**#74**), PR 2 (**#75**) und PR 3 (**#76**, samt Review-Fixes) alle gemergt. `temperature`/`max_output_tokens` haben `llm_provider_configurations` verlassen — `temperature` ist Beitrags-Einstellung des Mitglieds (vier feste Stufen 0.3/0.6/0.8/1.0, ausgeblendet bei einem `anthropic`-Provider), die Längengrenze eine betreibergepflegte **Zeichen**-Vorgabe je Ziel-Plattform (`text_generation_platform_defaults.max_characters`); der Ersteller wählt mehrere Ziel-Plattformen, beschränkt auf die, für die ein Kanal eingerichtet ist (`GET /v1/text-generation-platforms`, serverseitig mit 422 durchgesetzt), die knappste Vorgabe gibt die Länge vor. Alle drei Werte werden bei Sitzungsanlage eingefroren und gehen in den `input_hash` ein. `provider_parameter_hash` nimmt `temperature` nur auf, wenn der Adapter sie tatsächlich sendet. Step 4 (getrennte Texte je Plattform) am 2026-08-14 nach dem Review von PR #76 verworfen — die min()-Regel ist die Absicht, nicht die Notlösung (siehe Paket 043) |
| 043 | Einstellungen eines früheren Beitrags erneut laden | 032, 042 | vorgemerkt, noch nicht ausgeplant; Betreiberentscheidung vom 2026-08-14. Die Gegenseite zur min()-Regel: wer mehrere Plattformen gemeinsam anhakt, bekommt **einen** Beitrag mit der Grenze des restriktivsten Mediums (Blog + X ⇒ 280 Zeichen, auf beiden gleich veröffentlicht). Wer je Plattform einen eigenen Text will, erzeugt je Plattform eine eigene Sitzung — technisch heute schon möglich, weil `target_platforms` im `input_hash` steckt und dieselbe Materialbasis mit anderer Auswahl deshalb eine neue Sitzung ergibt. Was fehlt, ist die Bequemlichkeit: Anlass, Ziel, Quellmaterial, Stilprofil und Reglerstufe eines früheren Beitrags ins Formular zurückholen, dann nur die Plattformauswahl ändern und erneut erzeugen. Ersetzt das früher als „Pro-Plattform-Varianten und ihre Freigabe" vorgesehene Paket (`GeneratedPostSchema.variants` bleibt leer, die Freigabekette bleibt bei einer `post_version`) |
| 044 | [Zielplattform-Vorgaben je Ebene und harte Längengrenze](044-zielplattformen-vorgaben-und-harte-laengengrenze.md) | 042, 011 | erledigt: PR 2 (**#82**) und PR 1 (**#83**) beide gemergt. Die Zeichengrenze wird jetzt **hart** durchgesetzt — `assertCaptionLength` (packages/content-engine) prüft `caption` nach der Generierung gegen die eingefrorene `maxCharacters`, ein zu langer Entwurf löst genau einen internen, kandidatenslot-neutralen Wiederholversuch im Worker aus, danach ein echter Fehlschlag; `GeneratedPostSchema.caption` teilt sich jetzt denselben Wert wie `MaxCharactersSchema`s Obergrenze. `targetPlatforms` verliert den Contract-Vorgabewert und wird Pflichtfeld; `policy_settings.default_target_platforms` als vererbte Richtlinie (`null` = geerbt, `{}` = ausdrücklich keine, ein gesetzter Wert ersetzt statt einzuschränken — eigene Merge-Semantik `mergeReplaceableList`); `GET /v1/text-generation-platforms` liefert `isDefault` mit der Verfügbarkeit geschnitten, `erstellen.vue` hakt nur noch das an; neue Komponente `DefaultTargetPlatformsPicker.vue` in `einstellungen/index.vue`. Der interaktive Browser-Durchlauf (Vereinsvorgabe setzen, in einer Abteilung „geerbt“ sehen, dort abwählen) ist nicht automatisiert verifiziert |

Empfohlene Reihenfolge: 026 und 030 können parallel erfolgen. Danach 027, dann 028 und 029 parallel. **026, 027, 028, 029, 030 sind alle erledigt.** **028** war bereits erledigt, bevor die noch laufende `app.ts`-Zerlegung abgeschlossen war — die betroffenen Web-Seiten und der API-Client ließen sich unabhängig davon vereinheitlichen, ohne deren Ergebnis vorwegzunehmen. **031 ist erledigt.** Der vorhandene In-Prozess-Rate-Limiter bleibt bis zu einer bewussten Skalierungsentscheidung ein dokumentiertes Single-Instance-Limit; die veraltete historische Einleitung dieses Indexes wird in einem separaten Dokumentationsdurchgang bereinigt, damit diese fünf Code-Pakete keine fremde Plan-Historie umschreiben.

Hinweis zu 014: der Integrationsrahmen (`packages/integrations`: `planSync`, Transport-/Bereichs-Interfaces, Datei- und iCal-Transport) und `packages/member-directory` als erster Bereich sind fertig und getestet — ein HTTP-Adapter für ein konkretes Drittsystem ist bewusst **nicht** enthalten (kein Zielsystem mit dokumentiertem Testzugang), bleibt aber als eigener Spike vorgesehen (Enum-Wert `http` existiert, kein Code dahinter). `public.directory_people` trägt bereits `profile_id`/`joined_at`/`became_adult_at` und ist über `consent_records.directory_person_id` mit der Medienwelt verknüpft — Paket 015 kann direkt darauf aufbauen. Paket 019 hat `packages/integrations` erwartungsgemäß für Mannschaften/Spielpläne/Veranstaltungen wiederverwendet (neues Package `packages/club-schedule`, drei Bereichsadapter), ohne den Abgleichmechanismus selbst anzufassen; für einen HTTP-Adapter gilt weiterhin derselbe Spike-Vorbehalt. Bewusst nicht gebaut: eine tatsächliche Anwendung von `take_incoming`/`keep_current` auf `directory_people` (Konfliktauflösung vermerkt bislang nur die Entscheidung, siehe Plan 014 „Umsetzung: Ergebnis und Abweichungen“) sowie die automatische Prüfmarkierung veröffentlichter Beiträge bei nachträglich erkannter Minderjährigkeit (fehlt weiterhin an der Inhalts-Pipeline, siehe unten). Ausgehende Abrufe einer vom Verein hinterlegten Adresse laufen ausschließlich über `@vereinsfunk/outbound-fetch` (Zieladressenprüfung gegen interne Netze, Zeit- und Größengrenze) — jeder künftige HTTP-Adapter und jeder andere Abruf einer solchen Adresse gehört durch dieselbe Stelle, nicht durch ein nacktes `fetch()`. Geprüft ist das heute durch Durchsicht der Aufrufstellen im nicht generierten Quellcode (`apps/api`, `packages/`), nicht durch eine automatisierte CI-Prüfung; feste, im Code hinterlegte Provider-Endpunkte (Meta-Graph-API in `packages/publishing`) fallen nicht unter diese Regel, weil dort keine vom Verein gesetzte Adresse angefragt wird.

Hinweis zu 015: schließt das seit der ersten Content-Pipeline-Migration offene Medien-Gate wirklich (`evaluateConsent`/`scanTextForSensitiveData` in `packages/domain/src/consent.ts`, neue `consent.manage`-Permission, `consent_requests` für den digitalen Weg, `organization_consent_texts` für den pro Verein editierbaren Text). Adversarial beim eigenen Review gefunden und an drei Stellen behoben: `POST /v1/consent-requests` übernahm `recipientRole` ungeprüft — eine Anfrage mit `recipientRole: 'self'` an eine minderjährige Person hätte nach Zustimmung eine ohne jede Erziehungsberechtigten-Bestätigung als gültig geltende Einwilligung erzeugt (`evaluateConsent`s `guardian_missing`-Blocker prüft nur `signerRole === 'guardian'`, nie `directory_people.is_minor`) — derselbe Fund gilt für die Papier-Registratur und die Ablösung, alle drei prüfen jetzt `is_minor` gegen die gewählte Rolle. Bewusst nicht gebaut: die „Übernahme aus Quellsystem“ (`origin='imported'`, kein Zielsystem mit Testzugang, wie der HTTP-Adapter aus 014) und die Sprachmodell-Hälfte der Textprüfung (`SafetyFlagSchema.sensitive_data` bleibt unbefüllt, bräuchte einen LLM-Aufruf im Worker, den es ohne die Inhalts-Pipeline 001–007 nicht gibt). Details in `plans/015-einwilligungsverwaltung.md`, Abschnitt „Umsetzung: Ergebnis und Abweichungen vom Plan“.

Hinweis zu 020: macht Paket 009/012/015 produktivreif — sechs Aufbewahrungsregeln (Rohmedien, Derivate, Audit-Events, abgelaufene Token, Einwilligungsnachweise, verwaiste Auskunftsbündel), Betroffenenrechte (Auskunft als signierter Export, Löschung mit ehrlich benannten Ausnahmen), Verarbeitungsdokumentation und Auftragsverarbeiter-Verwaltung, ein kryptografisch tatsächlich verifizierbarer Audit-Trail (`packages/secrets` `createChainSigner`, HKDF-abgeleiteter Schlüssel getrennt vom AES-Schlüssel der `SecretBox`) und ein öffentliches, serverseitig gerendertes Impressum je Verein (`GET /v1/organizations/:id/imprint`, unauthentifiziert, verlinkbar aus der Social-Media-Bio). Der vierte kritische Befund oben (Cross-Tenant-Storage-Zerstörung über den Retention-Lauf) ist der schwerste von insgesamt zwölf adversarial gefundenen und behobenen Funden dieses Pakets — eine ausführliche, mit Regressionstests belegte Liste steht in `plans/020-rechtliche-pflichten-und-datenschutzbetrieb.md`, Abschnitt „Umsetzung: Ergebnis und Abweichungen vom Plan“. Bewusst nicht gebaut: vollständige, irreversible Vereinskonto-Löschung (eigenständige Funktion, verdient eine eigene sorgfältige Umsetzung statt eines Nachtrags); automatisierter Netzwerktest (keine Browser-Test-Infrastruktur im Repository); die fachliche Hatchet-Scheduler-Anbindung für Retention-Lauf und Audit-Signatur (Paket 004 stellt nun die technische Grundlage, beide Läufe bleiben bis dahin manuell auslösbar, inklusive protokollierten Trockenläufen).

## Fünfte Serie: Textwerkstatt als Kern-UX

Stand: 2026-08-10, geplant auf `5fccd6fa`. Dieser vertikale Ausbau macht die Erstellung von Beiträgen zum primären Produktfluss. Er generiert ausschließlich Texte und unterstützt optional eigene Fotos oder Videos. Videos werden bevorzugt auf dem Endgerät komprimiert; falls das Gerät den Zielcodec nicht zuverlässig erstellen kann, erfolgt nur nach transparenter Nutzerentscheidung ein privater Quarantäne-Upload mit serverseitiger Komprimierung. KI-Videoerzeugung und multimodales Bild-/Videoverstehen sind nicht Teil der Serie.

| Nr. | Arbeitspaket | Abhängigkeiten | Status |
|---|---|---|---|
| 032 | [Mobile Textwerkstatt mit Stilprofilen sowie Foto- und Video-Anhang](032-mobile-textwerkstatt-mit-stilprofilen.md) | 002, 004, 025, 030 | in Arbeit; Teilphase 1 (Verträge, tenant-sicheres Datenmodell, Provenienz, RLS-Tests, ADR-010) erledigt. Der ID-only Outbox-/Worker-Pfad aus 004 ist nachgewiesen; Medienanhänge bleiben bis 002 gesperrt. |
| 033 | [Echten Textgenerator und Provider-Routing als USP-Pilot](033-echter-textgenerator-und-provider-routing.md) | 032, laufender Outbox-Dispatcher aus 004 | erledigt; aktiviert ausschließlich sichere Textgenerierung. Bild-/Video-Generierung bleibt ein separates, provider-spezifisches Vorhaben. |
| 037 | [Plattform-kuratierte Personas für die Textwerkstatt](037-plattform-personas-fuer-stilprofile.md) | 022, 032 | erledigt; setzt die in Paket 032/ADR-010 bewusst ausgeklammerte Persona-Katalog-Erweiterung um: neue globale Tabelle `platform_style_personas` (kein `organization_id`, statt `content_style_profiles` zu erweitern, siehe Plan „Ausgangslage"), Admin-CRUD im bestehenden `/plattform-admin`-Panel, additive `personaSlug`-Auswahl neben Basismodi und Vereinsprofilen. Inhaltliche Kuration realer Personen ausdrücklich eigene, spätere Aufgabe. |
| 040 | [Personas/Stilprofile: Charakter-Modell statt Dial-Formular](040-personas-charaktermodell-statt-dial-formular.md) | 037 | erledigt; ersetzt das abstrakte Dial-Schema (Satzlänge/Energie/Humor/Formalität/Perspektive) durch ein konkretes Charakter-Modell (Tonalitäts-Tags, Catchphrases, Do's/Don'ts, Few-Shot-Beispielpaar) für Plattform-Personas **und** Vereins-Stilprofile, plus Live-Preview. Drei PRs alle gemergt: PR 1 (**#64**, Migration & Contracts), PR 2 (**#65**, PATCH/DELETE für Vereinsprofile, Preview-Endpunkte), PR 3 (**#67**, geteilte Editor-Komponente `StyleProfileEditorForm.vue`, neue Vereins-Seite `/stilprofile`) — alle drei Szenarien (Plattform-Admin-Persona, Vereinsmitglied mit `post.create`, Mitglied ohne Recht) per Playwright gegen den echten lokalen Stack verifiziert |

Reihenfolge: Paket 004 ist mit einem lokalen registrierten Handler nachgewiesen. **033** ist als text-only USP-Pilot erledigt: Provider-Routing, strukturierte LLM-Evaluation, Kandidaten vor akzeptierten Versionen und mobiler Mitgliederfluss stehen. Als Nächstes **032** für Foto-/Video-Anhänge vervollständigen (Plan 002 wird dafür benötigt, blockiert den bereits gelieferten Textpilot aber nicht). Bild- und Videoerzeugung sind ausdrücklich nicht Teil dieser Reihenfolge und benötigen je Provider einen eigenen Spike. Die Stilvorgabe ist als sichtbares, attributbasiertes Profil konzipiert; benannte Persona sind nach ADR-010 erlaubt und organisatorisch über Rollen und Freigabe abzusichern – **037** liefert dafür die admin-verwaltete Katalog-Ebene, **040** macht beide Formulare (Plattform-Persona und künftiges Vereinsprofil) für Nicht-Linguisten konkret bedienbar.

### Kritischster Befund

`apps/api/src/app.ts:66-72` ist die gesamte Autorisierung der API. `requireAuth` prüft nur, **ob** ein `authorization`-Header vorhanden ist, und das ausschließlich bei `NODE_ENV === 'production'`. Der Inhalt wird nie gelesen, keine Signatur geprüft, keine Permission ausgewertet. In Entwicklung und Test ist jeder Endpunkt offen. Das behebt Paket 008 und ist der Grund, warum es zuerst kommt. **✓ Behoben in Paket 008**: echte JWT-Verifikation und `requirePermission` an allen Endpunkten, die Scope-Daten in der Anfrage tragen.

Zweitwichtigster Befund: `social_connections` gewährt `authenticated` `select` auf die ganze Tabelle einschließlich `token_ciphertext` (`202608030001:125,131`). **✓ Behoben in Paket 012**: Token wandern in `social_connection_secrets` (kein Grant/keine Policy für `authenticated`), `social_connections` erhält einen spaltenweisen Grant ohne die Token-Spalten. Details in `plans/012-kanaele-und-social-accounts.md`, Abschnitt „Umsetzung: Ergebnis und Abweichungen vom Plan“.

Dritter Befund, adversarial in Paket 011 gefunden: `public.request_approval` (per RPC direkt für `authenticated` erreichbar, wie jede privilegierte Funktion in diesem Projekt) übernahm `stages` inklusive `reviewerSnapshot` sowie `self_approval_allowed`/`allow_same_reviewer_across_stages` ungeprüft vom Aufrufer — jede Person mit `post.submit` hätte eine fremde `userId` als Prüfer eintragen, mit leerer Stufenliste jede Prüfung inklusive der Minderjährigenstufe umgehen und sich selbst freigeben können. **✓ Behoben in Paket 011**: die Funktion berechnet die sicherheitsrelevanten Werte selbst aus `policy_settings` und prüft jede genannte Person gegen echte Vereinsmitgliedschaft. Details in `plans/011-regelwerk-richtlinien-und-kontingente.md`, Abschnitt „Umsetzung: Ergebnis und Abweichungen vom Plan“.

Vierter Befund, adversarial in Paket 020 gefunden: `media_assets.object_path` ist seit der ersten Content-Pipeline-Migration ein freier Text ohne CHECK gegen `organization_id`. Ein Mitglied mit `post.create` im eigenen Verein A konnte per `INSERT` eine Zeile mit `organization_id = A`, aber `object_path` im Ordner eines fremden Vereins B anlegen; der nächste Retention-Lauf von Verein A hätte damit ein echtes Storage-Objekt von Verein B (u. a. Einwilligungsnachweise, veröffentlichte Medien) unwiderruflich gelöscht, weil `POST /v1/organizations/:id/retention/run` mit Service-Role löscht. Vor Paket 020 folgenlos, weil kein Code je anhand dieser Spalte gelöscht hat. **✓ Behoben in Paket 020**: `select_expired_raw_media`/`select_expired_media_derivatives` geben nur Pfade zurück, die tatsächlich mit dem Präfix des angefragten Vereins beginnen. Details in `plans/020-rechtliche-pflichten-und-datenschutzbetrieb.md`, Abschnitt „Umsetzung: Ergebnis und Abweichungen vom Plan“.

Fünfter Befund, adversarial in Paket 016 gefunden, projektweit relevant: `apps/api/src/auth.ts`s `rolesForScope` prüft `organization_memberships`, `department_memberships` und `team_memberships` vollständig unabhängig voneinander — an keiner Stelle wird geprüft, dass die drei per Scope übergebenen IDs überhaupt zusammengehören. Ein Aufrufer mit einer echten Abteilungsrolle im eigenen Verein A hätte `organizationId=<fremder Verein B>` mit der eigenen, echten `departmentId` aus A kombinieren können: `requirePermission` ging über die reale Abteilungsrolle in A durch, obwohl die Anfrage inhaltlich Verein B betraf. Folgenlos blieb das bislang nur, weil jeder bestehende Endpunkt seine Daten zusammengesetzt nach `organization_id` UND `department_id` filtert und ein inkonsistentes Paar so zufällig leer liefert — die neue Kontingentauslastung in `GET /v1/analytics/summary` filterte `channel_quotas` dagegen ausschließlich nach `organizationId` und hätte echte Konfigurations- und Nutzungsdaten eines fremden Vereins zurückgegeben. **✓ Behoben in Paket 016**: `assertAnalyticsScopeConsistency` prüft vor jeder Rechteprüfung über den Nutzer-Client (RLS), dass eine angegebene `departmentId` tatsächlich zur `organizationId` gehört und eine `teamId` zur `departmentId`. Der gemeinsam genutzte `RoleProvider` selbst wurde bewusst **nicht** geändert — jeder andere bestehende Endpunkt, der `toPermissionScope(organizationId, departmentId)` aus rohen Query-Parametern bildet und danach irgendeine Abfrage nur nach `organizationId` filtert (nicht zusammengesetzt), könnte demselben Muster unterliegen; das verdient eine eigene, gezielte Prüfung außerhalb dieses Pakets. Details in `plans/016-auswertung-interne-kennzahlen.md`, Abschnitt „Umsetzung: Ergebnis und Abweichungen vom Plan“.

Sechster Befund, gefunden beim Code-Review von PR #40 (Paket 032), betrifft Paket 015: der dort neue `post_generation_provenance_immutable`-Trigger feuerte ursprünglich auf `before update or delete`, obwohl `post_generation_provenance.post_version_id` mit `on delete cascade` auf `post_versions` verweist — das Löschen einer `post_versions`-Zeile (und damit kaskadierend eines Posts oder einer ganzen Organisation, etwa bei einer DSGVO-Löschanfrage) wäre fehlgeschlagen, sobald Provenienz existiert. In der ersten Review-Runde als „akzeptiertes Muster, exaktes Vorbild `organization_consent_texts_immutable`“ (Paket 015) durchgewunken, in einer späteren Runde als eigenständiger Kritisch-Fund erkannt. **✓ Behoben in Paket 032**: Trigger auf `before update` beschränkt. `organization_consent_texts_immutable` selbst hatte exakt denselben Fehler — siehe siebter Befund unten.

Siebter Befund, wie der sechste beim Code-Review zu PR #40 gefunden, betrifft ebenfalls Paket 015: `organization_consent_texts_immutable` war identisch fehlerhaft angelegt (`before update or delete`) und blockierte damit dieselbe `ON DELETE CASCADE`-Kaskade beim Löschen einer Organisation mit vorhandenem Einwilligungstext. **✓ Behoben in Paket 015**: Trigger auf `before update` beschränkt, die Löschsemantik trägt der Fremdschlüssel; zwei pgTAP-Assertions in `consent_management.test.sql` beweisen die erfolgreiche Kaskade.

## Rückbau-Inventar: jeder Prototyp-Datensatz und sein Ersatz

Vollständige Liste der erfundenen Daten im Anwendungscode, mit dem Paket, das sie ersetzt. Kein Eintrag darf ohne Ersatz verschwinden und keiner ohne Rückbau bleiben.

| Ort | Was dort erfunden ist | Ersetzt durch |
|---|---|---|
| `apps/web/app/composables/useDemoData.ts` | Organisation „SV Nordstadt 1921“, vier Abteilungen als Strings, vier Beitragsentwürfe mit Datum als Text | ✓ 008: Datei vollständig gelöscht; alle drei Konsumenten (`layouts/default.vue:5`, `pages/index.vue:4`, `pages/beitraege.vue:3`) auf `useSession()`/`useScope()` umgestellt; `drafts` zum benannten Empty State, bis 009/010 echte Daten liefern. |
| `layouts/default.vue:5,43-57` | Organisation und Abteilungsliste aus dem Demo-Composable | ✓ 008 (Identität und Scope aus `useSession()`/`useScope()`), 010 (Abteilungsverwaltung/-CRUD steht noch aus) |
| `layouts/default.vue:81` | „Lena Müller / Social Managerin“ hartkodiert | ✓ 008 |
| `layouts/default.vue:13` | `badge: 2` bei Freigaben | ✓ 008 (kein Badge statt erfundener Zahl) |
| `pages/index.vue:5` | `firstName = 'Lena'` | ✓ 008 |
| `pages/index.vue:4` (`drafts`) | „Aktuelle Beiträge“-Liste aus dem Demo-Composable | ✓ 008 (Empty State), 009/010 (echte Liste steht noch aus) |
| `pages/index.vue:10-15` | vier Kennzahlen inkl. „Reichweite 24,8k +18 %“ | ✓ 009 (drei echte Zählwerte: Veröffentlicht, Offene Freigaben, Geplant nächste 7 Tage) — 016 hat hier bewusst nichts ergänzt, die Auswertungsseite selbst deckt weitere Kennzahlen ab; 017 (Reichweite) |
| `pages/index.vue:17-25,32` | erfundene Woche, „Sonntag, 2. August“ | ✓ 009 (echte `scheduled_for`-Daten der laufenden Woche in Vereinszeitzone, echtes formatiertes Datum), ✓ 019 (Anlassvorschläge-Karte im Dashboard) |
| `pages/index.vue:77-86` | statische „Idee für diese Woche“, toter `?type=`-Parameter | ✓ 009 (Nächste-Schritte-Karte aus `organization_brand_profiles`/`organization_profiles`/`organization_onboarding`), ✓ 019 (Anlassvorschläge-Karte — kein Rückbau eines Dummys, da 009 die Stelle bereits ersatzlos geräumt hatte, sondern eine neue, datengetriebene Funktion an ihrer Stelle) |
| `pages/index.vue:88-95` | „18 / 24 Beiträge“, „3 / 4 Abteilungen aktiv“, fester `w-3/4`-Balken | ✓ 009 (entfällt ersatzlos), 016 |
| `pages/beitraege.vue:3` | Liste aus dem Demo-Composable | ✓ 008 (Empty State), 010 (echte Liste steht noch aus) |
| `pages/erstellen.vue:14` | `useState('content-scope')`, das nirgends gesetzt wird | ✓ 008 |
| `pages/erstellen.vue:25-29,41,47` | `localPreview()` erzeugt eine Vorschau ohne API und ohne Persistenz | ✓ 008 (entfällt vollständig) |
| `pages/erstellen.vue:12` | leere Felder `title`, `date`, `location` bei jedem Beitrag neu | ✓ 019 (vorbelegt aus Spiel/Veranstaltung über `?fixtureId=`/`?clubEventId=`, mit Herkunftsangabe; nur bei einem Aufruf mit einem der beiden Anlass-Parameter) |
| `pages/freigaben.vue:3-6` | zwei erfundene Beiträge, „Minderjährige · Einwilligung geprüft“ als Text | ✓ 011 (echte Stufen aus `GET /v1/approval-stages/mine`, ehrlicher Leerzustand statt Fakedaten — ursprünglich 015 zugeordnet, 011 baute den dafür nötigen Mechanismus schon mit) |
| `pages/freigaben.vue:7,12` | Freigabe nur im lokalen State, kein Serveraufruf | ✓ 011 (`POST /v1/approval-stages/:id/decide`) |
| `pages/kalender.vue:1` | fest „August 2026“, fünf Fantasietermine, hartkodierte Vorlauftage | ✓ 009 (echte `posts.scheduled_for`, navigierbarer Monat, Empty State — der Plan zu 009 listete diese Zeile bereits in seinem eigenen Rückbau-Abschnitt, diese Tabelle hier nicht; beim Abgleich in der Adversarial-Phase von 009 nachgezogen), ✓ 019 (zweite/dritte Ebene `fixtures`/`club_events` neben den Beiträgen, Lücken ohne Beitrag verlinkt direkt in die Erstellung) |
| `pages/auswertung.vue:1` | vier erfundene Plattformkennzahlen, `bars`-Array ohne Skala und Quelle | ✓ 016 (echte interne Kennzahlen: Kennzahlenzeile, Zeitreihe mit echter Achse, Aufschlüsselung, Funnel, Kontingentauslastung — alles live aus den Rohtabellen berechnet), 017 (der benannt leere Bereich „Reichweite und Interaktionen“ bleibt bis dahin bestehen) |
| `pages/marke.vue:1` | Farben und Tonalität im lokalen State, „Speichern“ setzt nur ein Flag | ✓ 009 (`PUT /v1/organizations/:id/brand`, echter Ladezustand, echte Fehler), ✓ 013 (Scope-Umschalter, Asset-Verwaltung, Schrift-Upload mit Lizenzbestätigung, Live-Vorschau, Abteilungs-/Mannschaftsbranding) |
| `pages/mitglieder.vue:1` | vier hartkodierte Namen, „Einladen“ ohne Handler | ✓ 010 (echte Mitglieder/Rollen/Einladungen aus der Datenbank) |
| `pages/einstellungen.vue:1` | fünf behauptete Einstellungen, jeder Button ohne Handler | ✓ 011 (Freigabe, Minderjährigenschutz, Kontingente — echte scopeabhängige Richtlinienseite), ✓ 012 (Kanäle: eigene Seite `pages/kanaele.vue`, die hartkodierten Zeilen waren bereits mit 011 verschwunden), ✓ 020 (echte Löschfrist in `retention_settings`, durchgesetzt durch den bis zur separaten Scheduler-Anbindung manuell ausgelösten Retention-Lauf, mit Löschprotokoll; eigene Seite `pages/einstellungen/recht.vue`) |
| `apps/web/nuxt.config.ts:14-21` | Schriften von `fonts.googleapis.com` bei jedem Aufruf | ✓ 013 (selbst gehostete kuratierte Schriften, kein Drittanbieter-Aufruf mehr), ✓ geprüft in 020 (kein automatisierter Netzwerktest, manuell auf Start-/Anmelde-/Recht-/Impressum-Seiten bestätigt) |
| `packages/config/src/index.ts:17-20` | `PUBLISHING_PROVIDER: 'mixpost'`, Mixpost-URL und -Token — widerspricht der Meta-Entscheidung | ✓ 012 (`'fake' \| 'meta'`, `META_APP_ID`/`META_APP_SECRET`/`META_GRAPH_VERSION`/`META_OAUTH_REDIRECT_URL`) |
| `packages/domain/src/index.ts:47-87` | `mergeEffectiveConfig` existiert korrekt, wird aber nur von Tests aufgerufen | ✓ 011 (aus `apps/api/src/app.ts` über `resolveEffectiveConfig`/`computeRuleEntry` aufgerufen, kein Testpfad mehr) |
| `post_versions.effective_config_snapshot` | Spalte ist `not null` und wird von nichts gefüllt | ✓ 025 (`POST /v1/submissions` befüllt sie jetzt beim Anlegen einer `post_version` — geflacht unter `config`, siehe Plan 025 „Ausgangslage“, sonst wäre die von `schedule_publication`/`available-channels` gelesene Form verfehlt und die Kanal-Beschränkung aus 011/012 stillschweigend wirkungslos) |
| `evaluateMediaGate` `consentValid` | Feld wird von keinem Aufrufer befüllt | ✓ 015 (`evaluateConsent` in `packages/domain/src/consent.ts` befüllt ihn; verdrahtet in `GET /v1/approval-stages/mine`, praktisch erst mit einer echten `post_versions`-Zeile pruefbar — Inhalts-Pipeline 001–007 fehlt weiterhin) |
| `SafetyFlagSchema` `sensitive_data`, `minor`, `missing_consent` | drei von vier Flags werden nirgends bestimmt; `FakeContentGenerator` setzt ausschließlich `uncertain_fact` (`packages/content-engine/src/index.ts:40`) | ✓ 015 nur die regelbasierte Hälfte (`naming_not_allowed`/`sensitive_text_data` als neue `MediaGateBlocker`, kein Sprachmodell — siehe Plan 015 „Bewusst vereinfacht“); `SafetyFlagSchema.sensitive_data` selbst bleibt unbefüllt, das bräuchte einen LLM-Adapter im Worker (001–007). 002 (Medien) weiterhin offen. |
| Bucket-Größen ohne Vereinsgrenze | `file_size_limit` begrenzt eine Datei, nicht die Summe; ein Verein kann unbegrenzt viele 100-MB-Videos hochladen | 021 |
| `WorkflowNameSchema` `collect-analytics` | Name reserviert, Workflow nicht implementiert | 017 |
| `apps/api/src/app.ts:29-32` | `LocalUploadService` liefert `https://storage.invalid/...` | 002 (008 hat die Route nur mit echter Autorisierung versehen, den Stub aber nicht ersetzt — bleibt offen) |
| `apps/api/src/app.ts:70-100` | `/v1/submissions` persistiert nichts | ✓ 011 (echtes `insert` in `submissions`, `evaluateSubmitPermission` davor), ✓ 025 (bei vollständigem Quellmaterial entsteht jetzt echt ein `post`/eine `post_version`, inkl. `post_variants` und `assertGroundedPost`-Prüfung) |
| `README.md:35` | „funktioniert im lokalen Demo-Modus auch ohne Datenbank und API“ | ✓ 008 (Aussage entfernt) |

Zu den Zeilenangaben: die **offenen** Zeilen dieser Tabelle zeigen auf den aktuellen Stand und sind gegen den Code geprüft. Die mit ✓ markierten Zeilen beschreiben einen Zustand, den Paket 008 beseitigt hat — ihre Zeilennummern beziehen sich auf den Baseline-Commit `b5c2eda6` und zeigen heute auf anderen oder gelöschten Code. Sie bleiben als Nachweis stehen, was ersetzt wurde, und sind nicht als Sprungziel gedacht.

`supabase/seed.sql` bleibt bewusst erhalten. Es ist ein **Entwicklungs-Seed** für den lokalen Stack und wird in den Paketen 009, 010 und 014 erweitert — Paket 019 hat es entgegen der ursprünglichen Planung nicht angefasst (kein Teil seines Scopes, keine Seed-Daten für Mannschaften/Spiele/Veranstaltungen). Es darf nie Datenquelle für Anwendungscode sein und nie in Staging oder Produktion laufen — das steht bereits in seiner ersten Zeile und gilt weiter.

### Regel für den Rückbau

> Kein erfundener Wert wird durch einen Platzhalter, eine Null oder einen grauen Balken ersetzt. Existiert eine Zahl noch nicht, steht dort ein benannter leerer Bereich mit Begründung. Eine Null ist eine Falschaussage; ein leerer Bereich ist eine Information.

## Übergreifende Regeln der zweiten Serie

Ergänzend zu den Regeln der ersten Serie:

- **Vererbung verschärft nur.** Verein setzt den Rahmen, Abteilung und Team dürfen ausschließlich strenger werden. Eine untere Ebene hebt keine Pflicht auf und erweitert keine Erlaubnis. Umgesetzt in `resolveEffectiveConfig`, nicht in jedem Endpunkt neu.
- **Jeder Knoten kann eine Prüfstufe für alles unter sich verlangen.** Die Freigaberoute eines Beitrags ist die geordnete Kette dieser Stufen von innen nach außen, sequenziell abzuarbeiten. Stufen sind additiv: jede Ebene kann eine hinzufügen, keine entfernen.
- **Eine Befreiung von der Prüfpflicht wirkt nur nach unten.** Was auf Abteilungsebene gewährt wird, entfällt Abteilungs- und Teamstufen, niemals die Vereinsstufe.
- **Keine Befreiung entfällt die Minderjährigenstufe.** Das ist die Bedingung dafür, dass Spieler und Eltern überhaupt selbst einreichen dürfen: Vertrauen gilt der Person, nicht dem Risiko für Dritte.
- **Eine Permission gilt nur im Scope ihrer Rolle und für Ziele auf oder unterhalb dieser Ebene.** Ein Abteilungsadmin verändert nichts in einer fremden Abteilung. Umgesetzt in `assertScopedPermission`.
- **Niemand vergibt eine Rolle, die mächtiger ist als die eigene.**
- **Prüfer dürfen abteilungsfremd sein, sehen aber nur die Version.** Ein Marketing-Prüfer erhält Zugriff auf Text und freigegebene Derivate des zu prüfenden Beitrags — nie auf Rohmedien, Gesichtsregionen oder Personendaten.
- **Kein Format wird ungeprüft durchgereicht.** SVG wird geparst, gegen eine Allowlist gefiltert und neu serialisiert; ausgeliefert wird ausschließlich das Ergebnis.
- **Zeit gehört in die Vereinszeitzone.** Speicherung bleibt `timestamptz` in UTC; Tagesgrenzen, Kontingentperioden und Anzeigen richten sich nach `organizations.timezone`.
- **Kein Import löscht und kein Import ändert die Struktur.** Fehlende Datensätze werden stillgelegt, unbekannte Abteilungen erzeugen Konflikte, jede Übernahme ist zweistufig mit Trockenlauf.
- **Datenminimierung durch Schema.** Was ein Zod-Schema an der Integrationsgrenze nicht kennt, kommt nicht ins System. Das ist keine Disziplinfrage.
- **Geheimnisse verlassen die Service Role nicht.** Tokens, Zugangsdaten und Einwilligungsnachweise sind für `authenticated` nie lesbar.
- **Fristen werden von Jobs eingehalten, nicht von Vorsätzen.** Jede Aufbewahrungszusage in der Oberfläche braucht einen Cron und einen Test.

## Entschiedene Produktfragen

| Frage | Entscheidung | Betrifft |
|---|---|---|
| Datenquelle der Auswertung | zweistufig: interne Kennzahlen zuerst, Plattform-Insights hinter derselben Grenze nach dem App Review | 016, 017 |
| Einwilligungen | Registratur für Papiererklärungen **und** digitaler Einwilligungsprozess, plus Übernahme aus Drittsystemen | 015 |
| Drittsysteme | nicht nur Personen: Mannschaften, Spielpläne, Ergebnisse und Veranstaltungen; viele Quellen über einen gemeinsamen Rahmen statt Einzelimporte | 014, 019 |
| Schriftarten | kuratierte Auswahl als Standard **und** Upload eigener Schriften mit Lizenzbestätigung | 013 |
| Sentimentanalyse | eigenes späteres Paket mit eigenem Rechts-Gate | 018 |
| Freigabe | jeder Knoten bestimmt für alles unter sich, wer einreichen darf und ob geprüft wird — bis auf die einzelne Person. Mehrstufige Route: Trainer → Medienverantwortliche → Marketing. Prüfer dürfen abteilungsfremd sein. Minderjährigenstufe ist unbefreibar. | 011 |
| SVG-Logos | unterstützt, aber nur nach Allowlist-Sanitisierung mit eigenem Modul `packages/svg-safe` und Testkorpus bekannter Payloads | 009, 013 |
| Schriftformate | TTF, OTF und WOFF2 werden angenommen (legacy WOFF/Version 1 bewusst nicht, siehe Plan); Original nach `raw-media`, ausgeliefert wird nur das serverseitig konvertierte WOFF2 | 013 |
| Abteilungs- und Mannschaftsbranding | jede Ebene darf eigene Logos und Schriften führen, Default `true`; was eine Abteilung oder Mannschaft hochlädt, ist für andere nicht nutzbar | 013 |
| Speicher und Tarife | drei Tarife als Daten (kostenlos 3 GB / Einstieg / Premium), Preise und Grenzen ohne Deployment änderbar, operative Übersteuerung im Einzelfall; Verein teilt seinen Speicher auf Abteilungen und Mannschaften auf | 021 |
| Volle Speichergrenze | neue Uploads blockiert, geplante Veröffentlichungen laufen weiter, **nichts wird automatisch gelöscht** | 021 |
| Handgepflegte Datensätze | sind keine Integrationsquelle: `source_id = null` statt eines `manual`-Transports | 014 |
| Sensible Angaben im Beitragstext | zweistufig — regelbasiert blockierend (Kontaktdaten, IBAN, Namen), Sprachmodell nur beratend mit `sensitive_data`. Ein Modell erteilt keine Freigabe und verhindert keine allein. | 015 |
| Weggeklickte Anlassvorschläge | Zeitstempel am Ereignis statt eigener Tabelle; erscheinen wieder, sobald `source_updated_at` neuer ist | ✓ 019 |
| Verlegte Spiele | nur Hinweis am Beitrag plus Invalidierung offener Freigaben, kein automatisches „überholt“-Markieren eines bereits veröffentlichten Ankündigungsbeitrags | ✓ 019 |

## Offene Entscheidungen der zweiten Serie

Diese Punkte sind in den jeweiligen Paketen begründet und brauchen eine Festlegung, bevor das Paket umgesetzt wird:

- **Freigabe ganz abschaltbar?** Paket 011 legt „keine Prüfstufe auf keiner Ebene“ so aus, dass der Autor selbst veröffentlichen darf — es bleibt eine menschliche Entscheidung, nur keine zweite. Vollautomatisches Publizieren ohne Menschen bleibt ausgeschlossen. Diese Auslegung berührt die dokumentierte Produktgrenze und sollte bestätigt werden.
- ~~**Jahr des 18. Geburtstags**: Paket 014 speichert nur das Geburtsjahr. Empfehlung ist, das gesamte Jahr als minderjährig zu behandeln und im Zweifel die strengere Freigaberoute zu nehmen.~~ **Entschieden 2026-08-07**: das ganze Kalenderjahr des 18. Geburtstags gilt als minderjährig, umgesetzt in Paket 014 (`deriveIsMinor` in `packages/member-directory`, `recompute_directory_minor_status()` in der Migration).
- **Prüfer verlässt den Verein oder fällt aus**: Paket 011 markiert betroffene Stufen als `stalled` und erlaubt ein bewusstes, auditiertes Neuauflösen der Route. Ob es zusätzlich eine automatische Eskalation an die übergeordnete Ebene nach Fristablauf geben soll, ist offen. Eine automatische **Freigabe** nach Fristablauf ist ausgeschlossen.
- ~~**Einwilligungstext je Verein oder global** (Paket 015)~~ **Entschieden (2026-08-08): pro Verein editierbar**, umgesetzt in Paket 015 (`organization_consent_texts`, `text_version` = Zeilen-`id`, nie ein Update).
- ~~**Aufbewahrung von Einwilligungsnachweisen**~~ **Entschieden (2026-08-08): fünf Jahre ab Ende der Gültigkeit** (Plan-Vorschlag übernommen). **✓ Durchgesetzt seit Paket 020**: `select_expired_consent_evidence` löscht die Nachweisdatei nach `consent_evidence_years` ab Widerruf bzw. `valid_until`, die `consent_records`-Zeile selbst bleibt als Nachweis bestehen (nur `evidence_path` wird `null`).
- **E-Mail-Versand**: eigener Anbieter oder Supabase Auth Invite (Paket 010). Empfehlung eigener Versand, weil Einladungen auch an bestehende Nutzer gehen. Beschaffungsentscheidung.

- **Zahlungsdienstleister und Rechnungsstellung** (Paket 021): Stripe, Mollie und Paddle unterscheiden sich vor allem darin, wer Händler ist und wer die Umsatzsteuer schuldet. Dazu Kleinunternehmerregelung, fortlaufende Rechnungsnummern, SEPA-Mandate und das Verhalten bei ausbleibender Zahlung. Beschaffungs- und Steuerentscheidung, kein Implementierungsdetail.
- **Preise und Deckungsrechnung** (Paket 021): 20 € und 50 € sind Platzhalter mit Vorzeichen. Speicher, Rendering und LLM-Aufrufe sind die drei Kostenposten, und der teuerste ist nicht der Speicher. Vor dem ersten zahlenden Verein braucht es eine Deckungsrechnung je Tarif.
- **Bestandspreise** (Paket 021): ändert man den Preis in `subscription_plans`, gilt er für alle Bestandsvereine. Wer garantierte Preise will, braucht eine Preishistorie — die Entscheidung ist vor der ersten Erhöhung billig und danach teuer.
- **Video im kostenlosen Tarif** (Paket 021): 3 GB sind nach etwa zehn Videobeiträgen voll. Entweder die Oberfläche sagt das vorher deutlich, oder der kostenlose Tarif lässt nur Bilder zu.
- ~~**Nutzungskontingent für KI-Textgenerierung und automatischer Recovery-Trigger** (Paket 021, betrifft Paket 035)~~ **Entschieden (2026-08-13, vierte Runde): das Kontingent misst gar nicht mehr an der Textgenerierung.** Drei Zwischenstände sind durchlaufen und wieder verworfen worden: rohe Tokenzahl, dann Kosten in Cent (je Modell/Provider unterschiedlich teuer je Token), dann Beitragsanzahl je Instagram-Format (Feed-Bild/Karussell/Story/Reel — verworfen, weil „Karussell“ kein für einen Verein verständlicher Begriff ist). Die vierte Runde zählt stattdessen Beiträge nach **Medienherkunft** und Kalendermonat, gemessen beim Einplanen zur Veröffentlichung (`schedule_publication`): eigene Beiträge, KI-Bilder, KI-Videos mit Längenobergrenze — trifft zugleich die tatsächliche Kostenstruktur (KI-Erzeugung teurer als eigenes Material) und die Verständlichkeit. Nebeneffekt bleibt über alle Runden gültig: ein Recovery-Versuch (Paket 035) erreicht diese Zählung strukturell nie, weil er höchstens einen Textentwurf erzeugt, nie eine `publications`-Zeile — keine Ausnahme nötig. KI-Bild-/Videokontingente sind spezifiziert, aber wirkungslos, bis ein eigenes Paket die Erzeugung tatsächlich baut (siehe `plans/021-abomodelle-und-speicherkontingent.md`).
- **Eigentümerschafts-Obergrenze auf 1 gesenkt** (Paket 022): Betreiberentscheidung 2026-08-13, `max_organizations_per_owner` künftig mit Standardwert 1 statt 3 auszuliefern — ein Account soll höchstens einen Verein besitzen. Umsetzung als eigene kleine Migration außerhalb von Paket 021, siehe dort „Risiken und offene Entscheidungen“.

Anwaltliche Prüfung ist für Einwilligungstext, Datenschutzerklärung, AVV und die Lizenzbestätigung für Schriften Voraussetzung, nicht Option. Der Beschaffungsvorlauf ist größer als der Entwicklungsaufwand.

## Architekturfluss

```text
Nuxt
  │ Auth-Sitzung / Nutzeraktionen
  ▼
Fastify API ───────────────► Supabase Auth + Postgres + RLS
  │                               │
  │ signierte Uploads             ├── private Originale
  │ OAuth / Webhooks              └── private, abgeleitete Medien
  │
  └── ID-basierter Trigger ─► Hatchet ─► TypeScript Worker
                                  │          ├── ContentGenerator
                                  │          ├── FaceDetector / ImageAnonymizer
                                  │          ├── Remotion
                                  │          └── SocialPublisher → Meta Graph API
                                  │
                                  └──── Status/Fehler nur über Worker/API zurück nach Supabase
```

## Übergreifende Regeln

- Keine erfundenen Vereinsfakten: Texte dürfen nur bestätigte Angaben, Beobachtungen und freigegebene Zitate verwenden. Fehlende Angaben werden als offen markiert.
- Originalmedien bleiben privat. Veröffentlichbar sind ausschließlich geprüfte, unveränderliche Derivate.
- Keine Gesichtserkennung oder biometrischen Profile; nur Gesichtserkennung im Sinn von Lokalisierung (Bounding Boxes).
- Jede Freigabe bindet exakt eine unveränderliche `post_version` und die Prüfsummen ihrer Medien-Derivate.
- Externe Aktionen sind idempotent, auditierbar und nach einem unklaren Provider-Ergebnis erst zu reconciliieren, nicht blind zu wiederholen.
- Jede mandantenbezogene Tabelle trägt `organization_id`, verwendet zusammengesetzte Fremdschlüssel und besitzt positive wie negative RLS-Tests.
- Kinder erfordern eine ausdrücklich bestätigte Medienentscheidung und die strengere Freigaberoute.
- Produkttexte sprechen von „Gesicht verdeckt“, nicht von automatisch hergestellter Rechtssicherheit. Kleidung, Kontext und Umgebung können weiterhin identifizieren.

## Globale Verifikation

Nach jedem Paket mindestens:

```bash
corepack enable
pnpm install --frozen-lockfile
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

Bei Datenbankänderungen zusätzlich:

```bash
pnpm db:start
pnpm db:reset
pnpm db:test
```

Wenn ein externer Sandbox-Zugang nötig ist, dokumentiert der Executor die manuell geprüften Fälle, verwendete API-Version, App-Berechtigungen und das Datum in dem im Paket genannten Evidence-Dokument. Geheimnisse, Tokens und echte Kindermedien dürfen niemals eingecheckt werden.

## Bewusst nicht eingeplant

- Eigener Postiz- oder Mixpost-Betrieb im MVP
- Hive als Agenten-Orchestrierung
- Vollautomatisches Publizieren ohne menschliche Freigabe
- Gesichtserkennung, Personenabgleich oder biometrische Datenhaltung
- Automatisches Face-Tracking in Videos im MVP
- Weitere Netzwerke vor nachgewiesenem Pilotnutzen
