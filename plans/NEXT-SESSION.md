# Prompt für die nächste Session

## Aktueller Refactoring-Stand (2026-08-10)

- Arbeite auf `worktree-plan-027-continue-mitglieder-einladungen`. PR gegen `main` wird am Ende dieser Session eröffnet (bündelt Mitglieder/Einladungen und Richtlinien/Freigaben, siehe Commits auf diesem Branch).
- Diese Session hat Paket 027 um zwei Domänen erweitert:
  - **Mitglieder und Einladungen** (`apps/api/src/routes/members.ts`): Mitgliederliste, Mitgliedschafts-CRUD, alle Einladungsrouten.
  - **Richtlinien und Freigaben** (`apps/api/src/routes/policies.ts`, 1.440 LoC — die bisher größte Extraktion): Policy-Settings/-Rules/-Reviewers, Member-Review-Trust, die vier Freigabe-Durchsetzungsstellen (request-approval/decide/reresolve/stalled), Post-Version-Approval-Detail, `approval-stages/mine`, Scheduling, `publications/:id/execute`, Media-Grants und Channel-Quotas (gehören inhaltlich zur Kontingentlogik aus Paket 011, nicht zu Kanäle/OAuth).
- `apps/api/src/routes/shared.ts` ist entsprechend gewachsen (jetzt 302 LoC): `fetchAllRows`/`fetchAllRowsForIds`/`resolveMembershipScope`/`toPermissionScope` (vorherige Session) plus neu `notExpiredFilter`, `isAnyMemberOfOrganization`, `checkRateLimit` (Modul-Singleton, ersetzt die alte In-Prozess-Map in `app.ts`), `createAuditRecorder` (Factory für `recordAuditEvent`, siehe unten), `CONSENT_RECORD_SELECT`/`ConsentRecordRow` sowie die komplette Regelauflösung (`POLICY_RULE_COLUMNS`, `PolicyRuleRow(s)`, `fetchPolicyRuleRows`, `ownPolicyRuleRow`, `toRuleOverride`, `computeRuleEntry`, `resolveScopedEffectiveConfig`, `fetchMemberTrust`).
- **Wichtige Erkenntnis für die nächsten Extraktionen**: mehrere scheinbar "policy-interne" Helfer werden auch von `POST /v1/submissions` (Inhalts-Pipeline, evaluateSubmitPermission) und von praktisch jeder verbleibenden Domäne (`recordAuditEvent`, `isAnyMemberOfOrganization`) gebraucht. Vor jeder weiteren Extraktion mit `grep -n "<funktionsname>("  apps/api/src/app.ts` prüfen, ob eine Funktion außerhalb der eigenen Zeilenspanne aufgerufen wird — sonst bricht app.ts beim Entfernen der lokalen Definition. `recordAuditEvent` ist deshalb jetzt eine Factory (`createAuditRecorder(supabaseClients)`); `app.ts` und jedes Route-Modul rufen sie einmal auf und behalten den vertrauten Zwei-Parameter-Aufruf `recordAuditEvent(request, event)`.
- `apps/api/src/app.ts`: 6.789 → 5.162 LoC in dieser Session (Ausgangswert vor Paket 027: 8.301 LoC). Verwaiste Importe nach jeder Extraktion entfernt (u. a. `canAssignRole`/`canRemoveRole`/`Permission`/`Role`-Typ, ein gutes Dutzend Contracts-Schemas, `resolveEffectiveConfig`/`mergeEffectiveConfig`/`resolveReviewRoute` u. a. aus `@vereinsfunk/domain`).
- Verifiziert nach jeder Domäne: `pnpm --filter @vereinsfunk/api typecheck`, `pnpm --filter @vereinsfunk/api test` (263/263 bestehen unverändert), `pnpm lint`, `pnpm check` (voller Workspace-Gate inkl. Build) — alle grün.
- **Abweichung vom Plan-Text weiterhin nicht behoben**: Step 3 sieht die Aufteilung von `app.test.ts` in `*.routes.test.ts` je Domäne vor. Das ist für keine der fünf bisher extrahierten Domänen geschehen — alle 263 Tests laufen weiterhin zentral. Siehe `plans/027-api-route-module-boundaries.md`, Abschnitt „Current state“.

## Nächste Schritte

1. Lies vollständig: `AGENTS.md`, `docs/product/implementation-plan.md`, `plans/README.md`, `plans/027-api-route-module-boundaries.md` (Abschnitte „Current state" und „Fortschritt Step 3").
2. Prüfe `git status --short --branch` und `git log --oneline main..HEAD -- apps/api`. Bestehende Änderungen bewahren.
3. Nächste Domäne laut Reihenfolge: **Kanäle, OAuth und Publishing** — umfasst `organizations/:id/channels`, `channels/:id` (PATCH/DELETE/verify/scopes), `channel-scopes/:id`, `organizations/:id/channel-policy`, `channels/connect/:platform/start`, `channels/connect/:platform/callback`, `oauth-pending/:id` (GET/select), `post-versions/:id/available-channels`. Nutzt bereits `isAnyMemberOfOrganization`/`recordAuditEvent`/`resolveMembershipScope`/`toPermissionScope` aus `routes/shared.ts` — vor dem Schneiden mit `grep -n "isAnyMemberOfOrganization(\|recordAuditEvent(" apps/api/src/app.ts` prüfen, welche Aufrufe in diesem Bereich liegen.
4. Danach: Integration/Verzeichnis/Einwilligung, Datenschutz, Analytics — Reihenfolge aus dem Plan.
5. Für Plattform-Administration + LLM-Provider-Verwaltung (Paket 022, direkt nach den Einladungsrouten in `app.ts`, alle `requirePlatformAdmin`-gated) und für `POST /v1/submissions` (Inhalts-Pipeline) fehlt weiterhin eine Zuordnung zu einer der acht ursprünglich benannten Domänen — vor der Extraktion entscheiden (eigenes Modul je Konzern oder Ergänzung einer bestehenden Domäne) und im Plan vermerken.
6. Nach jeder Extraktion: `pnpm --filter @vereinsfunk/api test`, `pnpm --filter @vereinsfunk/api typecheck`, `pnpm lint`; bei Abschluss der gesamten Domäne zusätzlich `pnpm check`. Verwaiste Importe (Contracts-Schemas, `@vereinsfunk/domain`-Symbole, `apiMappers.js`-Funktionen) nach jeder Extraktion prüfen — ESLint (`no-unused-vars`) findet sie zuverlässig, `tsc` allein nicht immer.
7. Erst wenn `app.ts` nur noch Bootstrap/Registrierung enthält (Richtwert < 500 LoC, siehe Done-Kriterien in `plans/027-api-route-module-boundaries.md`): Step 4 (zentraler Tenant-Scope-Guard) und die Testaufteilung aus Step 3 nachholen oder bewusst als Abweichung dokumentieren.
8. Vor jedem Commit `git diff --check`; dann committen und pushen. Paket 029 (Contracts/Domain-Exports) bleibt bis zum Abschluss von 027 zurückgestellt.

## Weiterer Ablauf

Paket 027 läuft (Route-Kontext, Brand/Organisation, Struktur, Mitglieder/Einladungen, Richtlinien/Freigaben erledigt; drei benannte Domänen + zwei ungeklärte Bereiche offen). Danach Paket 029; keine API-Verträge ohne begründete, dokumentierte Entscheidung ändern.
