# Prompt für die nächste Session

## Aktueller Refactoring-Stand (2026-08-10)

- Arbeite auf `worktree-plan-027-continue-mitglieder-einladungen` (neuer Worktree/Branch, PR noch nicht eröffnet). Vorgänger-PR [#35](https://github.com/haexor/vereinsfunk/pull/35) (Route-Kontext, Brand/Organisation, Struktur) ist bereits gemergt.
- Diese Session hat Paket 027 um die Domäne **Mitglieder und Einladungen** erweitert: `apps/api/src/routes/members.ts` registriert `GET /v1/organizations/:id/members`, die Mitgliedschafts-CRUD-Routen (`POST/PATCH/DELETE /v1/memberships*`) und alle Einladungsrouten (`GET/POST /v1/invitations*`, `POST /v1/invitations/accept`).
- Neu: `apps/api/src/routes/shared.ts` mit `fetchAllRows`, `resolveMembershipScope`, `toPermissionScope` — diese drei werden auch von noch nicht extrahierten Domänen (Richtlinien/Freigaben, Kanäle) gebraucht und bleiben deshalb zusätzlich in `app.ts` importiert (nicht dupliziert, anders als das bereits gemergte, lokal duplizierte `toPermissionScope` in `routes/organization.ts` — dort bewusst nicht nachträglich angefasst, siehe „chirurgische Änderungen“). `resolveInvitationScope`/`resolveScopeName` wurden dagegen vollständig nach `members.ts` verschoben, da sie ausschließlich dort gebraucht werden.
- `apps/api/src/app.ts`: 8.301 → 6.789 LoC. Verwaiste Importe entfernt (`canAssignRole`, `canRemoveRole` aus `@vereinsfunk/authorization` sowie alle nur dort gebrauchten Contracts-Schemas).
- Verifiziert: `pnpm --filter @vereinsfunk/api typecheck`, `pnpm --filter @vereinsfunk/api test` (263/263 bestehen unverändert), `pnpm lint`, `pnpm check` (voller Workspace-Gate inkl. Build) — alle grün.
- **Abweichung vom Plan-Text festgestellt, nicht behoben**: Step 3 sieht vor, nach jeder Domänen-Extraktion die zugehörigen Tests aus `app.test.ts` in ein `*.routes.test.ts` zu verschieben. Das ist weder für Brand/Organisation noch für Struktur noch jetzt für Mitglieder/Einladungen geschehen — alle 263 Tests laufen weiterhin zentral in `app.test.ts`. Diese Session hat das für Mitglieder/Einladungen bewusst nicht nachgeholt, um den PR-Diff auf die Routen-Extraktion zu beschränken; die Testaufteilung steht als eigener, noch nicht eingeplanter Schritt aus.
- `plans/027-api-route-module-boundaries.md` trug bislang keine laufende Fortschritts-Checkliste (nur die Step-1–4-Beschreibung und ungehakte Done-Kriterien) — ergänzt um einen Abschnitt „Fortschritt Step 3“, der ab jetzt bei jeder weiteren Domäne aktualisiert werden sollte. `plans/README.md` war beim Sessionstart noch auf dem Stand vor PR #35 (führte 027 als „bereit“); beides ist jetzt aktualisiert.

## Nächste Schritte

1. Lies vollständig: `AGENTS.md`, `docs/product/implementation-plan.md`, `plans/README.md`, `plans/027-api-route-module-boundaries.md` (Abschnitt „Fortschritt Step 3“).
2. Prüfe `git status --short --branch` und `git diff --stat 5fccd6fa..HEAD -- apps/api`. Bestehende Änderungen bewahren.
3. Nächste Domäne laut Reihenfolge: **Richtlinien und Freigaben** — umfasst mindestens `policy-settings`, `policy-rules`, `policy-reviewers`, `member-review-trust`, `post-versions/:id/request-approval`, `approval-stages/:id/decide`, `approval-requests/:id/reresolve`, `approval-requests/stalled`, `post-versions/:id/approval`, `approval-stages/mine`, `post-versions/:id/schedule`, `publications/:id/execute`, `media-grants/:token`. Vor dem Schneiden genau prüfen, welche dieser Routen `resolveMembershipScope`/`toPermissionScope` aus `routes/shared.ts` mitnutzen (mehrere tun das bereits, siehe `grep -n "resolveMembershipScope(\|toPermissionScope(" apps/api/src/app.ts`).
4. Danach: Kanäle/OAuth/Publishing, Integration/Verzeichnis/Einwilligung, Datenschutz, Analytics — Reihenfolge aus dem Plan. Für die Plattform-Administration (Paket 022, `me/platform-admin-status` bis `platform-admin/usage-metrics`, direkt nach den Einladungsrouten in `app.ts`) fehlt noch eine Zuordnung zu einer der genannten Domänen oder ein eigenes Modul — vor der Extraktion entscheiden und im Plan vermerken.
5. Nach jeder Extraktion: `pnpm --filter @vereinsfunk/api test`, `pnpm --filter @vereinsfunk/api typecheck`, `pnpm lint`; bei Abschluss der gesamten Domäne zusätzlich `pnpm check`. Verwaiste Importe (Contracts-Schemas, `apiMappers.js`-Funktionen) nach jeder Extraktion prüfen, wie in dieser Session.
6. Erst wenn `app.ts` nur noch Bootstrap/Registrierung enthält (Richtwert < 500 LoC, siehe Done-Kriterien in `plans/027-api-route-module-boundaries.md`): Step 4 (zentraler Tenant-Scope-Guard) und die Testaufteilung aus Step 3 nachholen oder bewusst als Abweichung dokumentieren.
7. Vor jedem Commit `git diff --check`; dann committen und pushen. Paket 029 (Contracts/Domain-Exports) bleibt bis zum Abschluss von 027 zurückgestellt.

## Weiterer Ablauf

Paket 027 läuft (Route-Kontext, Brand/Organisation, Struktur, Mitglieder/Einladungen erledigt; fünf Domänen + Plattform-Administration-Einordnung offen). Danach Paket 029; keine API-Verträge ohne begründete, dokumentierte Entscheidung ändern.
